<script>
    export let beforeURL
    export let afterURL
</script>

<img src={urlFor(companyData.logo).width(200).url()} alt="Logo"/>
<img src={urlFor(companyData.logo).width(200).url()} alt="Logo"/>